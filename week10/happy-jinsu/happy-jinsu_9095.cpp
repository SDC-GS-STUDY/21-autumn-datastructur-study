#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main(void)
{
	int testNum, testCase[11], sum = 0, i, j, num = 0, caseNum=0;

	scanf("%d", &testNum);
	for (i = 0; i < testNum; i++)
	{
		scanf("%d", testCase[i]);
		num++;
	}

	for (i = 1; i <= num; i++)
	{
	
		for (j = 0; 0; j++)
		{
			j += testCase[j];
		}
	}
}